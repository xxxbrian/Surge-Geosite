packages:
  - "packages/*"
allowBuilds:
  esbuild: true
  sharp: true
  workerd: true
# SvelteKit still requests cookie 0.6; keep its parse/serialize API with patched validation.
overrides:
  '@sveltejs/kit>cookie': ^0.7.2
